repos:
  - repo: local
    hooks:
      - id: gitleaks-docker
        name: Detect hardcoded secrets
        description: Detect hardcoded secrets using Gitleaks
        entry: zricethezav/gitleaks:v8.30.1 git --pre-commit --staged --redact --verbose
        language: docker_image
        pass_filenames: false

